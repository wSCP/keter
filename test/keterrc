#alt + ctrl + Escape
#    bspc quit

#super + @j
#    scpc quit

#alt + {_,shift + }{1-9,0}
#    bspc {desktop -f,window -d} ^{1-9,10}

#alt + {grave, Tab}
#    bspc desktop -C {backward,forward}

#alt + bracket{left,right}
#    bspc desktop -f {prev,next}

#alt + b
#    bspc desktop -B

#alt + space
#    bspc desktop -l next

#alt + super + k
#    bspc window -k

#alt + super + c
#	bspc window -c

#alt + p
#    bspc window -t floating;\
#    bspc window -t sticky
    
#alt + {s,f}
#	bspc window -t {floating,fullscreen}

#alt + {grave,Tab}
#	bspc {window,desktop} -f last

#alt + apostrophe
#	bspc window -s last

#alt + {o,i}
#    bspc control --record-history off; \
#    bspc window {older,newer} -f; \
#    bspc control --record-history on

#alt + y
#	bspc window -w last.manual

#alt + m
#	bspc window -s biggest

#alt + {_,shift +, ctrl +}{h,j,k,l}
#    bspc window -{f,s} {left,down,up,right}

#alt + {_,shift + }c
#	bspc window -f {next,prev}

#alt + ctrl + {h,j,k,l}
#	bspc window -p {left,down,up,right}

#alt + ctrl + {_,shift + }space
#	bspc {window -p cancel,desktop -c}


super + {h,j,k,l}
    echo {hello world, again, thrice, quartered}
    #bspc window -e {left -10,down +10,up -10,right +10}
    
#alt + super + shift + {h,j,k,l}
#	bspc window -e {right -10,up +10,down -10,left +10}

#alt + ctrl + {1-9}
#	bspc window -r 0.{1-9}

#~button1
#	bspc pointer -g focus

#alt + button{1-3}
#	; bspc pointer -g {move,resize_side,resize_corner}

#alt + @button{1-3}
#    bspc pointer -u

#alt + x
#    urxvt

#alt + f
#    firefox

#alt + e
#    chromium-browser --app=https://mail.google.com

#alt + shift + f
#    chromium-browser --app=https://facebook.com

#alt + shift + o
#    pcmanfm

#alt + r
#    xterm -e ranger

super + t
    timenowplease

super + button3
    transmission-gtk

#alt + Print
#    scrot %Y-%m-%d-%s_$wx$h.png -e 'mv $f ~/Pictures/'

# make sxhkd reload its configuration files:
#alt + Escape
#	pkill -USR1 -x sxhkd

#key + value
#    Xun_command_1

#key1 + value1
#    yun_command1\
#    zun_command2\
#    xun_command3\
#    aun_command4

#alt + {x,y}
#    derp

#NO
#   no
#NO
#   no
#NO
#   no

super + shift + k; super + {1-9}
    scpc window -e {left,down,up,right} 0.{0-9}

#super+{h,c,i}
#    YES

#super + {x,y,z}; ctrl + {0-9}
#    YES\
#    YES

#{super, ctrl}+{c, d}
#    YES

#{super,ctrl}+{a,b}
#    YES
